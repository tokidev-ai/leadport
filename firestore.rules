rules_version = '2';

// Placeholder — deny everything. Replaced with the real rules in LP-04
// (PLAN.md §6). Deliberately closed by default rather than open.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
