# Copy to .env.local and fill in real values. Never commit .env.local or any
# file containing real secrets — see CLAUDE.md §3.
#
# Empty for now: no env vars are read yet. This file is populated as later
# issues wire up Firebase (LP-03), error tracking (LP-07), etc. Every var the
# app reads at runtime must be listed here with a placeholder value.
